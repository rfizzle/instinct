# Thin stub — logic lives in rfizzle/concord (mod-release.yml).
name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    uses: rfizzle/concord/.github/workflows/mod-release.yml@master
    # Add modrinth-id under `with:` once the Modrinth project exists.
    with:
      curseforge-id: "1617731"
    secrets: inherit
